<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Contact Me</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    /* Classic, modern, and trendy background with a refined gradient */
    body {
      font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #1a1a3d, #4a1d7c);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      overflow: hidden;
    }

    /* Refined glassmorphism card with consistent spacing */
    .contact-card {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(15px);
      -webkit-backdrop-filter: blur(15px);
      padding: 40px;
      border-radius: 25px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
      width: 450px;
      text-align: center;
      color: #fff;
      animation: fadeIn 1.2s ease-in-out;
      transition: transform 0.4s ease, box-shadow 0.4s ease;
      border: 1px solid rgba(255, 255, 255, 0.15);
    }

    .contact-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 15px 50px rgba(0, 0, 0, 0.4);
    }

    h1 {
      margin: 0 0 30px 0;
      font-size: 32px;
      font-weight: 600;
      letter-spacing: 0.5px;
      text-transform: uppercase;
      background: linear-gradient(90deg, #ffffff, #d1d1ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    input, textarea {
      width: 100%;
      padding: 15px;
      margin: 12px 0;
      border-radius: 12px;
      border: 1px solid rgba(255, 255, 255, 0.2);
      outline: none;
      font-size: 16px;
      background: rgba(255, 255, 255, 0.15);
      color: #fff;
      transition: all 0.4s ease;
      font-family: 'Inter', sans-serif;
      box-sizing: border-box;
    }

    input::placeholder, textarea::placeholder {
      color: rgba(255, 255, 255, 0.65);
      font-style: italic;
    }

    input:focus, textarea:focus {
      background: rgba(255, 255, 255, 0.25);
      transform: scale(1.02);
      box-shadow: 0 0 10px rgba(255, 255, 255, 0.4);
      border-color: rgba(255, 255, 255, 0.5);
    }

    button {
      background: linear-gradient(135deg, #ff6f61, #d83a7e);
      color: #fff;
      padding: 15px 30px;
      border: none;
      border-radius: 12px;
      cursor: pointer;
      font-size: 17px;
      font-weight: 600;
      transition: all 0.4s ease;
      margin-top: 20px;
      text-transform: uppercase;
      letter-spacing: 1px;
      width: 100%;
      box-sizing: border-box;
    }

    button:hover {
      background: linear-gradient(135deg, #d83a7e, #ff6f61);
      transform: translateY(-4px) scale(1.03);
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.35);
    }

    .success {
      color: #34c759;
      font-weight: 600;
      margin: 0 0 20px 0;
      background: rgba(52, 199, 89, 0.25);
      padding: 10px;
      border-radius: 10px;
      border: 1px solid rgba(52, 199, 89, 0.3);
    }

    .error {
      color: #ff3b30;
      font-weight: 600;
      margin: 0 0 20px 0;
      background: rgba(255, 59, 48, 0.25);
      padding: 10px;
      border-radius: 10px;
      border: 1px solid rgba(255, 59, 48, 0.3);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(25px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Enhanced responsive design for modern devices */
    @media (max-width: 500px) {
      .contact-card {
        width: 90%;
        padding: 30px 20px;
      }

      h1 {
        font-size: 26px;
      }

      input, textarea {
        padding: 12px;
        margin: 10px 0;
        font-size: 15px;
      }

      button {
        padding: 12px 25px;
        font-size: 15px;
        margin-top: 15px;
      }
    }
  </style>
</head>
<body>
  <div class="contact-card">
    <h1>Contact Me</h1>

    <% if (message) { %>
      <p class="success"><%= message %></p>
    <% } %>

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form action="/contact" method="POST">
      <input type="text" name="name" placeholder="Your Name" required />
      <input type="email" name="email" placeholder="Your Email" required />
      <input type="text" name="subject" placeholder="Subject" required />
      <textarea name="message" placeholder="Your Message" rows="5" required></textarea>
      <button type="submit">Submit</button>
    </form>
  </div>
</body>
</html>